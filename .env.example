# ============================================================================
# ListPull Configuration File
# ============================================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to 'listpull.env' in the same directory
# 2. Fill in ALL required fields marked with [REQUIRED]
# 3. Run: sudo ./deploy/install.sh
#
# QUOTING RULES:
# - Use double quotes for values with spaces: STORE_NAME="My Card Shop"
# - No quotes needed for simple values: STORE_EMAIL=contact@example.com
#
# The installer will validate this file before proceeding.
# ============================================================================

# ============================================================================
# SECURITY [REQUIRED]
# ============================================================================

# JWT Secret - Used to sign authentication tokens
# [REQUIRED] Generate with: openssl rand -hex 32
# Must be at least 32 characters. Set once, never changes.
JWT_SECRET=

# ============================================================================
# STORE INFORMATION [REQUIRED]
# ============================================================================

# Your store's name (use quotes if it contains spaces)
# [REQUIRED] Example: STORE_NAME="Blast Off Gaming"
STORE_NAME="Blast Off Gaming"

# Contact email for customers
# [REQUIRED] Example: STORE_EMAIL=blastofflouisville@gmail.com
STORE_EMAIL=blastofflouisville@gmail.com

# Store phone number
# [REQUIRED] Format: XXX.XXX.XXXX (displays as (XXX) XXX-XXXX)
# Example: STORE_PHONE=502.963.5186
STORE_PHONE=502.963.5186

# Store address (use quotes if it contains spaces)
# [REQUIRED] Example: STORE_ADDRESS="7513 Outer Loop, Louisville KY 40228"
STORE_ADDRESS="7513 Outer Loop, Louisville KY 40228"

# Your domain name for HTTPS (e.g., list.blastoffgaming.com)
# [REQUIRED] The installer will obtain an SSL certificate for this domain.
# Your server must be reachable at this domain on ports 80 and 443.
DOMAIN=list.blastoffgaming.com

# ============================================================================
# ORDER SETTINGS
# ============================================================================

# Prefix for order numbers (e.g., LP-240215-AB12)
ORDER_PREFIX=LP

# Number of days orders are held before returned to inventory
ORDER_HOLD_DAYS=7

# ============================================================================
# EMAIL NOTIFICATIONS [OPTIONAL]
# ============================================================================
# Leave SMTP_HOST empty to disable email notifications.
# Customers can still check order status on the website.
#
# --- Gmail Setup ---
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate an app password for "Mail"
# 4. Use that 16-character password below
#
# Example:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=yourstore@gmail.com
#   SMTP_PASS=abcd efgh ijkl mnop
#   FROM_EMAIL=yourstore@gmail.com
#
# ============================================================================

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
FROM_EMAIL=

# ============================================================================
# DISCORD NOTIFICATIONS [OPTIONAL]
# ============================================================================
# Create a webhook in your Discord server:
#   Server Settings > Integrations > Webhooks > New Webhook
# Copy the webhook URL and paste it below.
# Leave empty to disable Discord notifications.

DISCORD_WEBHOOK_URL=

# Hour to send the daily digest (24h format, default: 10)
DISCORD_DAILY_DIGEST_HOUR=10

# Timezone for the daily digest (default: America/New_York)
DISCORD_DAILY_DIGEST_TIMEZONE=America/New_York

# Hours before an unworked order triggers a stale alert (default: 48)
DISCORD_STALE_ORDER_HOURS=48

# ============================================================================
# ADVANCED SETTINGS (usually no need to change)
# ============================================================================

# CORS origin for production (your domain, e.g., https://list.blastoffgaming.com)
# Leave empty in development. Required for production.
CORS_ORIGIN=https://list.blastoffgaming.com

# Application URL (used for password reset links in emails)
# Defaults to CORS_ORIGIN if not set. Example: https://list.blastoffgaming.com
APP_URL=https://list.blastoffgaming.com

# Server port
PORT=3000

# Database path
DATABASE_PATH=./data/listpull.db

# JWT token expiry - how long staff stays logged in (e.g., 7d, 24h)
JWT_EXPIRY=7d

# Upload limits
MAX_FILE_SIZE_MB=1
MAX_DECKLIST_CARDS=500

# API rate limiting (milliseconds)
SCRYFALL_RATE_LIMIT_MS=100
POKEMON_RATE_LIMIT_MS=200
AUTOCOMPLETE_DEBOUNCE_MS=200
